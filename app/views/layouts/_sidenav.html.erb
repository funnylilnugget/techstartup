<% if logged_in? %>
<nav role='navigation'>
  <div id="menuToggle">

    <input type="checkbox" />

    <span></span>
    <span></span>
    <span></span>

    <%user = User.find(session[:user_id])%>
    <ul id="menu">
      <%= image_tag(user.picture, class: "userimg") if user.picture.attached?%>
      <br> <br>
      <%= link_to 'View Profile & Posts', user_path(session[:user_id]) %><br>
      <br>
      <%= link_to 'Create a new post', new_post_path %>
    </ul>
  </div>
</nav>
<% end %>
